import Header from './header.mjs'
import List from './list.mjs'
import Notepad from './notepad.mjs'

/**
 * all custom elements to define
 * @type {Array<{ element: HTMLElement, name: string }>}
 */
export const elements = [Header, List, Notepad]
