export const defaultDocumentTitle = document.title

/**
 * sets the document title.
 * @param {string} title string to set the title to
 */
export const setTitle = title => {
  document.title = title
}
